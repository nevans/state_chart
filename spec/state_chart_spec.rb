RSpec.describe StateChart do
  it "has a version number" do
    expect(StateChart::VERSION).not_to be nil
  end

  it "does something useful" do
    skip "not there yet!"
    expect(false).to eq(true)
  end
end
